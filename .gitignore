# OS generated files
.DS_Store
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE / Editor files
.idea/
.vscode/
*.swp
*.swo
.cursor/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.env*
*.env
.venv
env/
venv/
ENV/
pip-log.txt
pip-delete-this-directory.txt
*.egg-info/
dist/
build/

# Poetry
.pypoetry/
# poetry.lock # Keep this commented out unless you DECIDE not to commit it

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json # Usually committed, ignore if not needed
yarn.lock # Usually committed, ignore if not needed

# Logs
*.log
logs/
*.log.*

# Secrets (Add any other specific secret files you use!)
*.key
*.pem
key.json # Uncomment if you have a file named key.json containing secrets
vertexai.json # Uncomment if this contains secrets
*.secrets.*
mcp_agent.secrets.yaml # If applicable

# Temporary/Backup Files
*~
*.bak
*.tmp

# Other specific directories seen (Add/remove as needed)
Quickchart-MCP-Server/ # Ensure this and others intended as submodules are NOT ignored
mcp-agent-repo/       # If this is just cloned repo, ignore it
mcp-agent/            # If this is just cloned repo, ignore it
mcp-filesystem/       # If this is just cloned repo, ignore it
mcp-fetch/            # If this is just cloned repo, ignore it
mcp-server-qdrant/    # If this is just cloned repo, ignore it
local-backend/qdrant_data/  # Ignore local Qdrant data
local-backend/memory_data/  # Ignore local memory data

# macOS specific folders from Finder/Path Bar
.file 