const fs = require('fs'); const path = require('path'); const userDataPath = '/Users/juanzhang/Library/Application Support/denker-app'; try { const localStoragePath = path.join(userDataPath, 'Local Storage', 'leveldb'); console.log('Checking if local storage exists:', fs.existsSync(localStoragePath)); if (fs.existsSync(localStoragePath)) { console.log('Local storage directory contents:'); console.log(fs.readdirSync(localStoragePath)); } } catch (err) { console.error('Error:', err); }
